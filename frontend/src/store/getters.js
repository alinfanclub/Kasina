
export default {
    // fectchNewList 는 이름을 지어준것, api fectchNewList  와는 다르다.
    // fectchNewsList(state) { 
    //     return state.news;
    // },
    itemData(state) {
        return state.itemList
    }
  }